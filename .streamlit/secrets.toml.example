# Kopieren nach .streamlit/secrets.toml (lokal) bzw. als Secret in
# Streamlit Community Cloud (App-Einstellungen -> Secrets) eintragen.
# secrets.toml selbst ist in .gitignore und wird NICHT committet.

APP_PASSWORD = "dein-passwort-hier"
ANTHROPIC_API_KEY = "sk-ant-..."
